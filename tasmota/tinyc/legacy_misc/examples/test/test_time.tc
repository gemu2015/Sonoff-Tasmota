// Test: Time functions — timestamp, convert, millis/micros precision
// No hardware dependencies

char buf[256];
int errors;
int tests;

void check(int condition, char name[]) {
    tests = tests + 1;
    if (condition) {
        sprintf(buf, "  PASS: %s\n", name);
    } else {
        errors = errors + 1;
        sprintf(buf, "  FAIL: %s\n", name);
    }
    printStr(buf);
}

void test_timestamp() {
    print("--- timeStamp ---\n");
    char ts[32];
    timeStamp(ts);
    int len = strlen(ts);
    check(len > 10, "timeStamp not empty");
    sprintf(buf, "  Current timestamp: %s\n", ts);
    printStr(buf);
}

void test_millis_micros() {
    print("--- millis / micros ---\n");
    int ms1 = millis();
    check(ms1 > 0, "millis > 0");

    int us1 = micros();
    check(us1 > 0, "micros > 0");

    // Verify millis advances
    delay(100);
    int ms2 = millis();
    int diff = ms2 - ms1;
    check(diff >= 90 && diff <= 200, "millis advances ~100ms");

    char msg[64];
    sprintf(msg, "  100ms delay measured: %d ms\n", diff);
    printStr(msg);
}

void test_time_convert() {
    print("--- timeConvert ---\n");
    char ts[32];

    // Convert epoch seconds to time string
    // 1708905600 = 2024-02-26 00:00:00 UTC
    timeConvert(ts, 1708905600);
    int len = strlen(ts);
    check(len > 5, "timeConvert produces string");
    sprintf(buf, "  Epoch 1708905600 = %s\n", ts);
    printStr(buf);
}

void test_secs_conversion() {
    print("--- secsToTime / timeToSecs ---\n");
    char ts[32];

    // Convert seconds to HH:MM:SS format
    secsToTime(ts, 3661);  // 1h 1m 1s
    sprintf(buf, "  3661 secs = %s\n", ts);
    printStr(buf);

    // Convert back
    strcpy(ts, "01:01:01");
    int secs = timeToSecs(ts);
    check(secs == 3661, "timeToSecs roundtrip");

    secsToTime(ts, 86399);  // 23:59:59
    sprintf(buf, "  86399 secs = %s\n", ts);
    printStr(buf);
}

void test_tasm_time() {
    print("--- Tasmota Time Variables ---\n");
    char msg[128];
    sprintf(msg, "  Date: %04d-%02d-%02d\n", tasm_year, tasm_month, tasm_day);
    printStr(msg);
    sprintf(msg, "  Time: %02d:%02d:%02d\n", tasm_hour, tasm_minute, tasm_second);
    printStr(msg);
    sprintf(msg, "  Weekday: %d CW: %d\n", tasm_wday, tasm_cw);
    printStr(msg);
    sprintf(msg, "  Sunrise: %d Sunset: %d\n", tasm_sunrise, tasm_sunset);
    printStr(msg);
    sprintf(msg, "  Epoch: %d\n", tasm_time);
    printStr(msg);

    check(tasm_time > 1700000000, "epoch > 2023");
}

int main() {
    errors = 0;
    tests = 0;
    print("=== Time Functions Test Suite ===\n\n");

    test_timestamp();
    test_millis_micros();
    test_time_convert();
    test_secs_conversion();
    test_tasm_time();

    sprintf(buf, "\n=== Results: %d tests, %d errors ===\n", tests, errors);
    printStr(buf);
}
