# Partition table the migrator EXPECTS to find on the target device,
# i.e. the historic Tasmota 2-app-OTA layout for 4 MB flash. It's also
# the partitioning the migrator binary itself is built against — we
# need the migrator to fit in `app1` (1856 KB) and we need the
# bootloader to load us from there after Tasmota's OTA hands off.
#
# Name,    Type, SubType,  Offset,  Size,    Flags
nvs,       data, nvs,      0x9000,  0x5000,
otadata,   data, ota,      0xE000,  0x2000,
app0,      app,  ota_0,    0x10000, 0x1D0000,
app1,      app,  ota_1,    0x1F0000,0x1D0000,
spiffs,    data, spiffs,   0x3C0000,0x40000,
